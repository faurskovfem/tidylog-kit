[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "tidylog-kit"
version = "0.3.1"
description = "Keep your log directories small without thinking about it"
requires-python = ">=3.9"

[project.scripts]
tidylog-kit = "logwash.cli:main"
